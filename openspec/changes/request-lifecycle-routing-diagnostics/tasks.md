## 1. 后端正确性修复（先处理 bug）

- [x] 1.1 修复 `transformPathRoutingDecisionLog` 候选 `circuit_state` 固定为 `closed` 的问题（输入：候选上游与 circuit breaker 实际状态；输出：路由决策候选状态与真实状态一致；验收：出现 `open/half_open` 时日志与详情展示不再误报为正常）
- [x] 1.2 统一失败路径的上游错误证据保留逻辑（输入：上游非 2xx 响应与异常分支；输出：状态码、错误消息、错误体摘要按统一结构落日志；验收：管理端可追溯“上游返回了什么”）
- [x] 1.3 收敛 `did_send_upstream` 与 `failure_stage` 判定边界（输入：未发往上游、已发往上游但失败、流式中断场景；输出：阶段语义稳定；验收：关键分支测试断言与语义一致）

## 2. 生命周期与诊断数据契约

- [x] 2.1 基于现有字段实现生命周期状态派生（输入：`status_code`、`routing_decision.did_send_upstream`；输出：决策中/请求中/完成成功/完成失败统一映射；验收：历史与新日志都能得到确定状态）
- [x] 2.2 扩展日志 API 响应字段承载阶段与错误摘要信息（输入：request-logger 返回对象；输出：向后兼容的新增字段；验收：旧字段不破坏，新字段可被前端直接消费）
- [x] 2.3 统一阶段耗时口径（输入：`duration_ms`、`routing_duration_ms`、`ttft_ms`；输出：决策、首 token、生成、网关处理等可计算片段；验收：无上游发送与流式请求均有可解释输出）

## 3. 后端测试与阶段门禁

- [x] 3.1 补充单元测试覆盖熔断状态展示正确性与失败证据保留（输入：proxy/load-balancer/request-logger 关键分支；输出：新增或更新测试用例；验收：相关测试全部通过）
- [x] 3.2 补充日志 API 与类型转换测试覆盖生命周期派生和新增字段（输入：transformer 与 list logs 场景；输出：接口契约测试；验收：兼容场景与新增场景均通过）
- [x] 3.3 执行后端阶段质量门禁并形成可提交状态（输入：阶段代码；输出：`pnpm lint`、`pnpm exec tsc --noEmit`、`pnpm test:run` 通过记录；验收：工作区满足阶段提交条件）

## 4. UI 单行步骤轨道改造（放在最后）

- [ ] 4.1 将日志主行改为单行横向步骤轨道（输入：生命周期状态与阶段耗时；输出：决策/请求/响应/完成四段单行展示；验收：默认无需展开即可读完主链路）
- [ ] 4.2 在响应段内联展示子耗时（输入：TTFT 与生成耗时；输出：同段内可读的子类型耗时文案；验收：流式请求可见首 token 与生成耗时）
- [ ] 4.3 在失败段内联展示错误摘要（输入：错误码、错误类型、错误摘要；输出：单行失败诊断信息；验收：用户可直接看到上游失败核心信息）
- [ ] 4.4 完成移动端紧凑化适配与 i18n 文案更新（输入：新轨道布局与新增文案 key；输出：中英文一致的紧凑展示；验收：移动端保持单步可读）

## 5. UI 回归与最终门禁

- [ ] 5.1 更新组件测试覆盖单行轨道与失败摘要显示（输入：logs-table 与 routing-decision 组件；输出：关键交互与展示断言；验收：新增测试稳定通过）
- [ ] 5.2 执行最终质量门禁并确认可实施状态（输入：全量改动；输出：`pnpm lint`、`pnpm exec tsc --noEmit`、`pnpm test:run` 通过；验收：OpenSpec 进入 apply-ready）
