## 1. 公共视觉与页面结构

本清单按依赖顺序执行。页面编号对应 `specs/application-ui-experience/spec.md`。所有页面任务均包含中英文业务文案审查及 `design.md` 的视觉矩阵：桌面／移动、明暗／中英文常态检查，关联操作和错误状态按实际行为覆盖。测试数据仅限本地受控环境。

所有页面任务必须按 `design.md` 的 M01–M12 感知体验调查及页面映射，覆盖实际可达的控件、输入、浮层、切换、等待、数据更新、操作结果、排序、滚动和设备适配。每项实际触点记录“已优化／已验证保留”，不适用说明原因；新发现的同页体验断点继续处理，不能局限于用户示例。每页验收包含完整操作与动态过程，不能以静态截图通过、只有入场动画或通用清单打勾作为完成依据。

任务满足自身验收后立即勾选；每组结束执行阶段集成检查并提交，仅提交本变更相关文件，使用 Conventional Commit 并运行仓库提交钩子。阶段检查复用已通过结果，只在后续改动影响时重跑。阶段截图与命令结果附在对应任务下，供最终收口复核。

- [x] 1.1 以现有 `--vr-*`、字体与图表主题为输入，调整明暗语义令牌和受控排版／边界；保留状态与数字契约，运行 `design-tokens-contrast`、`visual-style-guard` 及已有相关样式守护测试，确认正文对比度和受控样式通过。
- [x] 1.2 以 `PageShell`、`PageHeader`、`Topbar` 为输入实现共享页头与四类受控布局，说明和图标按需呈现；用组件测试验证无说明标题、主操作、宽列表与窄表单，并核对现有调用方兼容性。
- [x] 1.3 调整 `AppShell`、两角色布局和 `Sidebar`，保留滚动容器／折叠状态／底部安全区，补齐移动标题和详情返回；通过两角色布局测试和直接进入详情、前进后退、导航激活的浏览器检查。
- [x] 1.4 在既有 Button、Table、Dialog、Input、Select、Badge 与状态原语中应用统一规则，不新增平行组件库；验证键盘焦点、禁用、处理中、关闭恢复焦点与 reduced motion，确认刷新不重播整页动画。
- [x] 1.5 以现有 CSS／Radix／容器过渡为输入补齐双向展开收起与弹窗过渡，协调退出存在期、真实高度和焦点；扩展 `use-container-morph` 与相关组件测试验证快速反向、异步高度、源节点消失、清理和能力降级，并在日志详情／筛选区／创建弹窗实际观察完整进退过程。
- [x] 1.6 在现有导航／壳层边界实现轻量页面切换，保持公共导航、实时条和滚动模型稳定；通过 `app-shell` 及浏览器检查验证旧内容退出／新内容进入、同级与详情返回、快速导航、慢请求／错误和无增强能力，确认不冻结路由、不重复挂载或人为延迟导航。
- [x] 1.7 补齐标签／实例分区、编辑模式与异步内容的短过渡，复用局部实现而非新建全局动画状态机；通过对应交互测试和动态浏览器检查验证最新选择、草稿／焦点、取消清理、reduced motion 与真实内容不被截断。
- [x] 1.8 完成 M01/M02 的共享控件与输入反馈：按源码收窄产生意外运动的 `transition-all`，统一 hover／按压／focus／checked／disabled／pending，协调校验提示与按钮占位；验证键盘、触屏、输入法、粘贴、密码显隐、快速输入和失败草稿，保留已顺畅的行为。
- [x] 1.9 完成 M03 的菜单、Tooltip、Popover、Select、日期面板与对话框反馈协调；通过现有原语／页面测试和浏览器检查验证进退节奏、触发来源、边缘避让、嵌套层级、焦点恢复和长内容／软键盘，不统一更换浮层库。
- [x] 1.10 完成 M06 的主题／语言多入口体验：收敛独立 ThemeToggle、侧栏和移动菜单的主题反馈，保留 system 主题与查询参数；验证快速切换、reduced motion、语言长文本、已开浮层和遮罩清理，确认没有入口差异与强闪烁。
- [x] 1.11 完成 M07/M08/M10 的共享等待与结果表达，连接既有 query／mutation 状态与按钮、列表、空态、局部提示、toast；用受控快慢请求验证旧结果标识、局部刷新、连续保存／复制、真实错误、提示去重和关闭后清理，不添加虚构进度或人为等待。
- [x] 1.12 在既有图表／实时组件完成 M09 的动画与读数规则，保持数字／单位／颜色和真实结果；验证时间／指标／分组切换、轮询更新、tooltip、触屏与图表内部 reduced motion，动态检查不反复扫入、不影响持续阅读，并记录代表性长列表／图表的浏览器性能观察而非预先声称流畅。
- [x] 1.13 以 P02/P08/P07/P04 的真实受控内容检验公共基础，覆盖 M01–M12 的适用维度和桌面／移动完整操作过程；同时查看公开落地页，修正共享层确定性回归后完成阶段质量检查与提交，不将这四页提前标记为完整交付。

## 2. 登录与设置入口

- [x] 2.1 完成 P01 登录页：删除模拟启动、固定就绪标签与表单等待，使用新结构；更新 `login-page` 测试验证页面就绪可输入、两模式、真实失败／限流、密码管理器和角色／返回分流，并完成该页视觉与文案验收。
- [x] 2.2 完成 P17 设置页：统一系统入口与偏好操作，不遗漏用户、计费、任务、录制、规则、补偿、CLIProxyAPI；验证全部入口可达、语言与主题切换、退出行为及移动布局。
- [x] 2.3 集成验证 P01/P17 与公共导航在成员、管理员账号、管理员令牌会话下的行为，完成阶段质量检查与提交，并记录两页验收证据。

## 3. 管理与成员密钥

- [x] 3.1 完成 P04 列表、归属筛选与分页，统一加载、无数据、无匹配与失败状态；通过对应列表组件测试及桌面／移动视觉检查，确认已有筛选与详情入口有效。
- [x] 3.2 完成 P04 创建、一次性密钥结果和撤销弹窗；验证创建失败保留输入、一次性展示／复制、撤销确认及焦点返回，不在测试日志或截图中使用真实密钥。
- [x] 3.3 完成 P05 对象页头、分区导航、基础与有效期配置；通过相关组件测试与深链接／返回检查，验证局部保存错误保留草稿，完成该部分移动端检查。
- [x] 3.4 完成 P05 授权、模型、速率与消费规则分区；保留现有字段、校验与保存粒度，通过既有密钥详情／分区测试和完整页面视觉验收。
- [x] 3.5 完成 P19 成员密钥列表、创建／编辑／撤销及一次性值展示；通过 `portal-keys-table`、`portal-key-dialog` 和门户 E2E 验证上游可见／隐藏、管理员禁用、额度／速率收紧与权限失败，完成页面全部关联交互验收。
- [x] 3.6 联合验证 P04/P05/P19 的创建至详情／编辑、启停、撤销与返回流程，完成阶段质量检查与提交；三页及各弹窗均有视觉、文案和行为证据后才结束本组。

## 4. 上游工作台与详情

- [x] 4.1 完成 P06 列表、搜索、能力／状态筛选、密度与分页，保留原有控件能力；验证筛选组合、无匹配、刷新失败和两端可读性，完成列表部分视觉检查。
- [x] 4.2 完成 P06 创建、连接测试与删除弹窗；用受控响应验证成功、失败、超时／处理中及关闭焦点，不调用真实外部上游，完成弹窗视觉与文案检查。
- [x] 4.3 完成 P07 对象摘要、分区导航及名称、官网、端点、密钥、诊断分区；验证独立保存、敏感值处理、直接进入与返回、移动锚点不被遮挡。
- [x] 4.4 完成 P07 优先级权重、模型路由、计费倍率与消费配额分区；运行相关分区测试，验证数值格式、原有限制、保存失败草稿和不同主题布局。
- [x] 4.5 完成 P07 容量／队列、熔断、失败规则与会话迁移分区；验证实际状态表达、操作结果及全局／局部失败规则编辑器调用方兼容，完成上游详情整页验收。
- [x] 4.6 联合验证 P06/P07 的列表至详情、配置／测试、返回和全部关联操作，完成阶段质量检查与提交，确认没有遗留旧分区或弹窗。

## 5. 用户管理与个人用量详情

- [x] 5.1 完成 P09 用户列表、搜索／分页、创建、编辑、改用户名、重置密码与删除；通过现有用户管理组件测试验证最后一个启用管理员保护及管理员令牌例外、失败保留输入，检查主要弹窗布局。
- [x] 5.2 完成 P09 上游授权、批量可见性、密钥分配／查看相关弹窗；验证可见性、归属、跨分页操作反馈和关闭焦点，并完成用户列表整页视觉与文案验收。
- [x] 5.3 完成 P10 用户用量页并同步检查共享 `StatCard`／`PortalUsageChart` 的成员调用方；验证用户身份、指标时间口径、图表范围、错误与不存在状态、关联请求参数，完成页面视觉验收。
- [x] 5.4 集成验证用户创建至授权／分配、查看用量与已有请求入口的连续流程，完成阶段质量检查与提交，记录 P09/P10 及共享调用方验收结果。

## 6. 概览与排行榜

- [x] 6.1 完成 P02 统计、趋势、拓扑、排行摘要和既有下钻的布局，复用现有统计／图表原语；验证五类指标、组合图、sparkline、阈值告警和真实拓扑状态，完成该页视觉矩阵。
- [x] 6.2 完成 P18 成员概览，按现有数据提供创建密钥入口并区分零值、首次无数据、局部错误与刷新；通过 `portal-pages` 及门户 E2E 检查个人统计与时间口径，确认不展示虚构余额或接入成功。
- [x] 6.3 完成 P03 排行榜与关联请求窗口；通过 `rankings-table`、`rankings-url-state` 和排行榜 E2E 验证维度／排序／时间／搜索、深链接恢复，完成窗口和页面的两端视觉验收。
- [x] 6.4 集成验证 P02/P18/P03/P10 的共享图表、数字、时间范围和已有导航行为，完成阶段质量检查与提交，保留各页独立的数据作用域。

## 7. 双角色请求记录

- [x] 7.1 统一共享 `LogsTable` 的常用／高级筛选、区间统计、列层级和移动展示，并同步适配 P08/P20；用现有日志组件／查询测试验证防抖筛选合并、排序、分页、局部失败和无匹配状态。
- [x] 7.2 完成共享请求展开详情，包括请求过程、路由、Token、思考配置、性能与计费信息；补齐高度与显隐协调的双向过渡，动态检查异步内容、展开中收起／再展开及邻行位移；验证长字段、缺失数据、失败请求和移动阅读，保持现有信息能力与格式化口径。
- [x] 7.3 完成 P08 管理页实时条、连接／降级状态、刷新、录制关联与已有 URL 过滤；通过相关 live／日志测试和受控浏览器冒烟验证真实更新与失败反馈，完成管理页及展开区视觉验收。
- [x] 7.4 完成 P20 成员页个人查询、刷新和权限允许的详情；通过 `use-portal-logs`、门户组件测试与受控浏览器检查验证用户作用域和受限录制区不可见，完成成员页及展开区视觉验收。
- [x] 7.5 集成验证排行／用户详情至请求记录的已有对象及时间条件、详情返回与两角色差异，完成阶段质量检查与提交，确认共享修改没有泄露信息或丢失筛选。

## 8. 计费与后台任务

- [x] 8.1 完成 P11 汇总、近期计费、未匹配模型与价格目录布局；通过现有 billing 组件和 hooks 测试验证费用／单位／未定价状态、过滤与异步错误，完成两端列表视觉检查。
- [x] 8.2 完成 P11 价格覆盖编辑、重置与同步反馈；通过价格行、重置和计费 E2E 验证编辑失败、原有规则／权限、真实结果与既有数据口径，完成计费整页及弹窗验收。
- [x] 8.3 完成 P12 后台任务页与任务操作；通过 `background-sync-tasks-panel` 测试验证单任务运行中、成功、失败和重复点击处理，完成页面视觉与文案检查。
- [x] 8.4 验证 P11/P12 共享同步状态与操作反馈的集成行为，完成阶段质量检查与提交，不改变价格来源、调度和后端服务。

## 9. 录制与请求处理规则

- [x] 9.1 完成 P13 录制设置、过滤和记录列表；通过 `traffic-recording-page` 与 hooks 测试验证设置草稿、保存结果、时间／对象过滤和脱敏状态，完成两端视觉检查。
- [x] 9.2 完成 P13 内容详情、删除和过期清理交互；验证长 JSON 局部滚动、详情失败、危险操作确认与结果，使用受控记录，不删除真实数据，完成整页关联交互验收。
- [x] 9.3 完成 P14 全局失败规则页及共享编辑器视觉；通过 `global-failure-rules-page`、`upstream-failure-rules-editor` 测试验证匹配／动作／生效范围和保存，并回归 P07 的局部规则调用方。
- [x] 9.4 完成 P15 请求头补偿列表、匹配／能力／来源、规则顺序、启停、创建／编辑／删除；对 SourceList 补齐 M11 的拖拽来源／目标／落位／取消反馈及简单上移下移等非拖拽操作；通过现有补偿 hooks 和必要交互测试验证同一排序语义、键盘／触屏与失败状态，完成全部表单的两端视觉与文案检查。
- [x] 9.5 联合验证 P13/P14/P15 与既有日志／上游入口，完成阶段质量检查与提交，确认录制与规则能力没有因界面简化丢失。

## 10. CLIProxyAPI 与账户密码

- [x] 10.1 完成 P16 实例列表、当前实例上下文、创建／编辑／删除和连接测试；通过实例表单、列表与连接结果测试验证选择、失败、处理中和焦点，完成此部分两端视觉检查。
- [x] 10.2 完成 P16 账号列表、详情、字段、模型和 OAuth 交互；通过现有 `cliproxy-accounts-*`、账号详情与 OAuth 弹窗测试验证待授权、成功、失败／过期、取消和实例切换，使用受控响应。
- [x] 10.3 完成 P16 认证文件导入／导出／删除、池上游与关联上游操作；通过现有文件、池和关联组件测试验证结果、错误、确认、敏感信息与文件交互，完成弹窗视觉检查。
- [x] 10.4 完成 P16 实例日志和整页分区组织；通过实例日志测试及切换实例的浏览器检查验证内容作用域、长日志、刷新失败和移动操作可达，完成 CLIProxyAPI 全部关联界面验收。
- [x] 10.5 完成 P21 密码页；通过 `portal-change-password-form` 测试验证当前密码错误、确认不一致、长度、提交中和成功结果，完成窄表单、密码管理器与两端视觉检查。
- [x] 10.6 完成 P16/P21 阶段集成、质量检查与提交，记录外部 OAuth／真实账号未执行的验证边界，禁止将受控前端测试描述为真实服务授权通过。

## 11. 全页面收口

- [x] 11.1 对照 P01–P21 与 M01–M12 复核实际控件和完整操作链，记录已优化／已验证保留／不适用及理由，补齐调查后发现的同页感知缺口；逐页阅读中英文文案，确认无设计说明、宣传填充、虚构状态，同时保留安全与业务说明。
- [x] 11.2 补全现有 visual／a11y 工具中的路由与受控场景覆盖，完成设计规定的每页视觉矩阵及代表性关联操作、键盘／reduced motion 检查；另以短视频或浏览器过程观察完成动态验收矩阵，验证双向切换、中断、动态高度、快速导航、慢请求与能力降级，不能用关闭动画的截图代替；人工查看实际截图后再更新基线，并确认公开落地页无共享样式回归。
- [x] 11.3 跑通登录／接入、密钥管理、用户授权／用量、排行／日志、上游诊断、计费和 CLIProxyAPI 既有流程；验证三类登录身份、既有深链接、返回恢复、作用域与错误反馈，不请求真实付费模型服务。
- [x] 11.4 完成 `pnpm lint`、`pnpm format:check`、`pnpm exec tsc --noEmit`、`pnpm test:run`、`pnpm build`、相关 `pnpm e2e`、`pnpm test:a11y` 和 `pnpm test:visual`；修正本变更失败后重验，环境阻塞明确记录，不将未运行项目标记为通过。
- [x] 11.5 仅清理本次迁移后确认零引用的旧表现代码，先精确复核引用再删除，重跑受影响检查并完成最终阶段提交；复核全部任务证据、运行 OpenSpec 严格校验，交付完整页面覆盖与剩余限制，不自动 push、部署或归档。

## 执行证据（持续补齐）

- 2026-09-08，1.1／1.2：明暗语义令牌、图表主题与共享页头／宽度检查通过；最近一组 10 个测试文件、127 项检查通过，包含 design-tokens-contrast、visual-style-guard、radius-style-guard、chart-theme、page-header、page-shell、topbar。页面与交互任务仍按各自验收条件逐项完成。
- 公共动态实现进行中：application-motion E2E 的 3 项浏览器检查通过，覆盖日志真实高度逐帧变化、快速反向／焦点／清理、路由连续切换及历史返回、390px 移动端 reduced motion。动态采样发现浏览器将 220ms 令牌返回为 0.22s，已修正 WAAPI 时长换算。该证据不代表全部动态矩阵完成。
- 本地验证使用独立 data/ui-refactor.sqlite 和受控 API 响应，不请求真实付费上游。发现既有 SQLite 迁移与当前 schema 不一致（录制／计费等字段缺失）；仅补齐独立测试库继续前端验收，未修改仓库迁移或真实数据库。生产迁移一致性不在本次前端验证结论内。

### 页面验收与触点处置

下表的“优”表示本次已优化，“留”表示结合源码、已有行为测试与浏览器检查验证保留，“—”表示页面没有该类实际触点。M01–M12 的含义见 design.md。共享导航、原语与主题的优化计入各使用页面；不为没有图表或拖拽的页面添加效果。业务校验与权限沿用组件/API 测试，浏览器覆盖页面矩阵与下列代表性连续交互，二者不互相替代。

| 页面 | M01 | M02 | M03 | M04 | M05 | M06 | M07 | M08 | M09 | M10 | M11 | M12 | 页面证据与保留边界 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P01 登录 | 优 | 优 | 优 | — | 留 | 优 | 优 | — | — | 留 | — | 优 | 即时可输入、账号/令牌键盘切换、失败与三类身份分流；无集合、图表和排序 |
| P02 管理概览 | 优 | — | 优 | 留 | 优 | 优 | 优 | 留 | 优 | 留 | — | 优 | 统计/拓扑/图表、时间范围与局部错误；读数、单位和下钻契约保留 |
| P03 排行 | 优 | 留 | 优 | 优 | 优 | 优 | 优 | 留 | 留 | 留 | — | 优 | 维度、URL 恢复、时间窗口、关联请求及排行展开；数值排序保留 |
| P04 管理密钥 | 优 | 留 | 优 | 优 | 优 | 优 | 优 | 留 | — | 优 | — | 优 | 创建浮层、一次性值与复制、撤销、归属筛选/分页；配额详情双向展开 |
| P05 密钥详情 | 优 | 留 | 优 | 留 | 优 | 优 | 优 | 留 | — | 留 | — | 优 | 6 个分区逐个锚点与截图；独立保存、授权/模型/限额、失败保留已有表单 |
| P06 上游列表 | 优 | 留 | 优 | 优 | 优 | 优 | 优 | 留 | — | 留 | — | 优 | 搜索/能力/状态/密度、创建/测试/删除；分组及行展开，移动操作换行 |
| P07 上游详情 | 优 | 留 | 优 | 留 | 优 | 优 | 优 | 留 | — | 留 | — | 优 | 13 个分区逐个锚点与截图；独立配置、诊断、配额、队列、熔断与局部规则 |
| P08 管理请求 | 优 | 留 | 优 | 优 | 优 | 优 | 优 | 留 | 优 | 留 | — | 优 | 防抖合并、20 行逐帧展开/反向、真实连接降级、录制权限、日期窄屏浮层 |
| P09 用户管理 | 优 | 优 | 优 | 留 | 优 | 优 | 优 | 留 | — | 留 | — | 优 | 创建与 7 类操作浮层；键盘关闭返回菜单、管理员保护、授权/归属与可见性 |
| P10 用户用量 | 优 | — | 优 | — | 优 | 优 | 优 | 留 | 优 | 留 | — | 优 | 身份摘要、个人范围、不存在/查询错误、图表指标与关联请求参数 |
| P11 计费 | 优 | 留 | 优 | 优 | 优 | 优 | 优 | 留 | — | 留 | — | 优 | 价格目录/展开/编辑/同步、未定价表达；费用计算和保存粒度保持原契约 |
| P12 后台任务 | 优 | 留 | 优 | — | 优 | 优 | 留 | 留 | — | 留 | — | 优 | 单任务运行/配置/成功失败、重复点击保护；日期与操作在窄屏可读 |
| P13 录制 | 优 | 留 | 优 | 优 | 优 | 优 | 优 | 优 | — | 优 | — | 优 | 保存状态与草稿分离、查询/JSON/复制、删除及过期清理确认、详情失败重试 |
| P14 失败规则 | 优 | 留 | 优 | 留 | 优 | 优 | 留 | 留 | — | 留 | — | 优 | 既有全局编辑器与 P07 调用方共用原语；匹配示例、动作与生效范围保留 |
| P15 补偿规则 | 优 | 优 | 优 | 留 | 优 | 优 | 优 | 留 | — | 优 | 优 | 来源排序/键盘/触屏、IME、最新状态播报；保存失败保留草稿且重试后才关闭 |
| P16 CLIProxyAPI | 优 | 优 | 优 | 留 | 优 | 优 | 优 | 留 | — | 优 | — | 优 | 实例键盘切换、账号 5 类浮层、OAuth 等待/取消/重开、粘贴草稿、池与日志 |
| P17 设置 | 优 | — | 优 | — | 优 | 优 | — | — | — | 留 | — | 优 | 7 个系统入口、语言/主题/外链与退出；没有业务查询、集合、图表和排序 |
| P18 成员概览 | 优 | — | 优 | — | 优 | 优 | 优 | 留 | 优 | 留 | — | 优 | 真实个人统计、零值/错误区分、密钥入口；图表快速指标切换与单位保留 |
| P19 成员密钥 | 优 | 优 | 优 | 留 | 优 | 优 | 优 | 留 | — | 留 | — | 优 | 创建/编辑/启停/撤销、上游隐藏和额度收紧；移动表格与关联字段可读 |
| P20 成员请求 | 优 | 留 | 优 | 优 | 优 | 优 | 优 | 留 | 优 | 留 | — | 优 | 共享请求表与日期选择；个人数据范围、受限录制区不展示，语言保留查询 |
| P21 密码 | 优 | 优 | 优 | — | 优 | 优 | 优 | — | — | 留 | — | 优 | 当前/新/确认密码、显隐焦点与自动填充、提交错误/成功；无集合和排序 |

除 P15 来源列表以外均没有手工重排操作，M11 不适用；表头数值排序归入 M08。M09 仅覆盖实际图表/持续更新读数，其他页的普通状态标签不视为图表。M04 的“留”指原有分区/编辑显示已经有明确状态和正确焦点，未为了增加动画而额外改造。

### 集成与动态证据

- 公共层及全部页面的单元/组件回归：`pnpm test:run --maxWorkers=4`，249 文件、3659 通过、1 个既有跳过。日志为 `data/ui-refactor-unit-final.log`。后续日期布局改动另跑 `time-range-selector` 和 `user-keys-dialog`，2 文件 25 项通过；相关账号/用户表单 4 文件 16 项通过。
- `application-motion.spec.ts` 在正常动画条件下覆盖实际高度逐帧变化、连续反向、退出存在期/焦点/清理、连续导航及返回、reduced motion、无 View Transition API、慢筛选的旧结果与表格位置、图表最新选择、补偿保存失败重试、移动主题/语言连续切换。不是以关闭动画的截图代替动态验收。
- `application-overlays.spec.ts` 覆盖 6 类创建表单 × 2 主题 × 1440/320，24 次浮层边界、WCAG 和 Escape 焦点检查；另有用户 7 类/CLI 账号 5 类浮层在暗色 320 与亮色 1440 的实际菜单进入、关闭与焦点恢复。日期面板在 320/390 × 600 高度下检查无横向溢出、可关闭及焦点恢复。
- `application-details.spec.ts` 以暗色 390、亮色 1440 实际点击密钥 6 个、上游 13 个分区锚点，38 张分区截图均已人工查看。完整路径首屏矩阵为 21 × 2 语言 × 2 主题 × 1440/390，共 168 张；另外复核既有 1280 桌面 9 张与公开首页 4 张。截图基线先人工查看再更新，最终比较结果单独记录。
- `application-cli.spec.ts` 以 1440/320、明暗主题执行实例键盘选择、账号/日志作用域、OAuth 等待/取消/重开、文件/粘贴方式快速切换保留草稿、池配置浮层。真实 OAuth 授权、外部账号与真实文件服务没有执行；文件 API、错误、成功和取消生命周期使用已有组件/hook 测试与受控响应验证。
- 既有 E2E 继续覆盖账号/管理员令牌/成员分流、成员密钥限制、用户列表、密钥/上游详情及错误重试、排行 URL/请求窗口、路由日志和计费规则。后端权限/金额/配置语义未因表现层修改而替换。
- 本地动态观察：20 行日志展开与图表切换分别采集 24 帧，样本主体约 16–17ms，起始帧分别约 42ms/31ms；日志高度在约 220ms 内由 0 达到实际内容高度。此为本机开发服务器的短过程观察，不是跨设备性能承诺。原始采样保存在 `data/ui-refactor/motion-sample.json`、`chart-sample.json`，E2E 可重新生成。
- 复核中修正：移动弹窗进场的重复平移、菜单弹窗关闭失焦、只读用户名缺少标签、CLI 详情不能键盘滚动、删除按钮对比度、移动密钥名被挤断、日期按钮中文竖排与窄屏日期面板溢出。文件选择按钮原文声称支持拖放但没有对应处理，改为真实支持的“点击选择”。
- 实际截图保存在忽略目录 `data/ui-refactor/`，可提交基线位于 `tests/visual/*-snapshots/`；测试中仅出现明确的虚构数据。设备验证使用 Chromium 视口/触屏事件与缩小高度，未宣称真实手机软键盘、Safari 或真实密码管理器插件已经测试；标准 autocomplete 属性与焦点由组件/浏览器验证。

### 提交与最终质量门

实施阶段未逐组创建提交；收口按“共享层及全部业务页面实现与行为测试”和“视觉基线、无障碍矩阵及验收记录”两个集成提交保存，避免将当前混合改动描述为已存在的逐组历史。提交正常运行仓库钩子。

- 最终 `pnpm lint`、`pnpm format:check`、`pnpm exec tsc --noEmit`、`git diff --check` 通过。
- 最终 `pnpm e2e --workers=4`：77 通过；`pnpm test:a11y --workers=4`：104 通过；`pnpm test:visual --workers=4`：181 通过。最终日志分别为 `data/ui-refactor-e2e-final.log`、`data/ui-refactor-a11y-final.log`、`data/ui-refactor-visual-compare.log`。日期收口后的三个套件完整重跑，未把基线更新本身视为比较通过。
- `pnpm build` 成功，使用隔离的 PostgreSQL 构建配置及不可连接的本地测试地址，未读取生产数据库；日志 `data/ui-refactor-build.log`。此前的本任务开发服务器已停止。构建成功不等于生产数据库迁移或外部服务已验证。
- 构建同时报告 2 条 `Encountered unexpected file in NFT list` 警告，追踪链涉及 `next.config.ts`、录制服务与 instrumentation；这些后端/构建文件不在本次改动中。警告未阻止构建，文件追踪范围问题作为独立发现保留。
- `openspec validate rebuild-application-ui --strict` 通过。确认零引用后移除旧主题遮罩和日志分区入场样式；没有变更后端 API、数据库 schema、依赖或公开首页业务实现。
- 分支为 `feat/rebuild-application-ui`；仅本地提交，不 push、部署或归档。正常动画、降级、键盘与窄屏证据随测试保留；真实 OAuth/付费模型/生产数据和未运行的真实设备组合均不在通过结论中。
