## 1. 数据模型与配置

- [x] 1.1 新增后台同步任务状态表和执行历史表，更新 PostgreSQL schema、SQLite schema、迁移文件和 Drizzle 快照
- [x] 1.2 扩展 `model_discovery` 配置类型，新增后台自动刷新开关，并让历史上游默认视为关闭
- [x] 1.3 扩展环境变量配置解析，加入后台同步总开关、价格目录任务开关与间隔、模型目录任务开关与间隔、启动延迟配置
- [x] 1.4 补充数据库结构一致性检查和配置解析单元测试

## 2. 后台任务调度服务

- [x] 2.1 新建后台同步任务类型、任务运行结果类型和任务状态读写服务
- [x] 2.2 实现后台任务调度器，支持注册任务、启动定时器、启动延迟、下次计划时间计算和停止定时器
- [x] 2.3 实现同一任务进程内互斥，计划执行遇到运行中任务时记录跳过状态
- [x] 2.4 实现手动立即执行入口，运行中任务不并发、不排队，并返回当前运行状态
- [x] 2.5 新增 `src/instrumentation.ts`，仅在 Node.js server runtime 中启动后台任务调度器
- [x] 2.6 补充调度器单元测试，覆盖启动关闭、定时执行、跳过运行中任务、手动执行和状态持久化

## 3. 价格目录后台同步

- [x] 3.1 注册 `billing_price_catalog_sync` 任务，复用 `syncBillingModelPrices()` 执行价格目录和分层计费规则同步
- [x] 3.2 将价格同步结果转换为后台任务运行结果，记录成功数量、失败数量、失败原因和耗时
- [x] 3.3 确保现有手动价格同步接口继续可用，并更新可复用的任务状态展示数据
- [x] 3.4 补充价格目录后台同步测试，覆盖成功、失败保留旧价格、启动后首次执行和手动执行

## 4. 模型目录后台自动刷新

- [x] 4.1 扩展上游创建、更新、读取、API transformer 和管理端类型，使后台自动刷新开关完整读写和回显
- [x] 4.2 注册 `upstream_model_catalog_sync` 任务，查询 active 且显式开启后台自动刷新的上游
- [x] 4.3 对符合条件的上游逐个复用 `refreshUpstreamCatalog(id)`，成功时更新目录缓存，失败时保留旧缓存并记录上游失败状态
- [x] 4.4 将批量刷新结果转换为后台任务运行结果，支持 success、partial、failed 三种状态和失败摘要
- [x] 4.5 确保现有单个上游手动刷新接口不受后台自动刷新开关限制
- [x] 4.6 补充模型目录后台刷新测试，覆盖未开启不刷新、停用不刷新、手动刷新不受开关限制、部分失败和失败保留旧缓存

## 5. 管理端 API

- [x] 5.1 新增后台同步任务列表 API，返回任务名、展示名、启用状态、运行状态、最近执行状态、最近时间、失败原因、耗时和下次计划时间
- [x] 5.2 新增后台同步任务手动立即执行 API，并加入管理员认证、未知任务和运行中任务处理
- [x] 5.3 补充 API transformer 和前端类型定义，保持 snake_case API 与前端类型转换一致
- [x] 5.4 补充后台任务 API 单元测试，覆盖认证、查询、手动执行、运行中任务和未知任务

## 6. 管理端界面

- [x] 6.1 在系统管理区域新增后台任务状态面板，展示任务状态、最近成功、最近失败、失败摘要、耗时、下次计划时间和立即执行按钮
- [x] 6.2 在上游模型发现配置区域新增后台自动刷新开关，并在创建和编辑弹窗中保持草稿态、保存态和读取态一致
- [x] 6.3 调整 Billing 页面最近价格同步摘要，使其与后台价格同步任务状态保持一致
- [x] 6.4 补充 hooks 和组件测试，覆盖任务状态展示、手动执行刷新、失败摘要展示和上游自动刷新开关

## 7. 验证与交接

- [ ] 7.1 运行后台任务、价格目录、模型目录、管理端 API 和相关组件测试
- [ ] 7.2 运行 `pnpm exec tsc --noEmit`、`pnpm lint` 和数据库一致性检查
- [ ] 7.3 运行 OpenSpec 校验，确认 `background-catalog-sync` 的 proposal、design、specs 和 tasks 均满足要求
- [ ] 7.4 检查差异范围，确认未修改无关功能，并按任务完成节奏提交改动
