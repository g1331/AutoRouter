# 实施任务

## 1. 代理响应采集

- [x] 1.1 输入：现有非流式 JSON、SSE 解析路径；输出：`ProxyResult` 与 `StreamMetrics` 的可选响应模型数据，至多保留两个不同的流式模型且响应原样透传；验收：`proxy-client` 聚焦单测覆盖三类模型位置、事件跨分块、无效模型与原始字节，并运行该文件测试。

## 2. 成功结算与诊断日志

- [ ] 2.1 输入：任务 1 的模型数据和最终 `resolvedModel`；输出：成功的非流式与流式日志携带可选响应模型及差异标志，真实不一致时只发一次结构化警告，失败、断流、取消不误报；验收：代理路由及流生命周期测试验证匹配、别名、缺失值、失败与原响应及计费保持不变。

## 3. 管理端显示

- [ ] 3.1 输入：任务 2 的日志字段；输出：双语管理端桌面/移动警告徽标、解释提示和响应详情两模型值，会员和历史日志不显示；验收：组件测试及日志路由 E2E 覆盖显示、隐藏与详情。

## 4. 集成与交付准备

- [ ] 4.1 输入：前述功能及测试；输出：实际桌面/窄屏浏览器截图检查和隔离 SQLite E2E 结果，整体单测、lint、格式、类型、生产构建、迁移一致性及 OpenSpec 校验均通过；验收：保存命令结果并核对 UI 无裁切、正常记录无警告。
