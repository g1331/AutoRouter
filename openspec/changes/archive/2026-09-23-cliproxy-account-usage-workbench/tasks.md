## 1. 账号用量只读链路

- [x] 1.1 以 CLIProxyAPI `auth-files` 契约为输入，实现独立的账号用量投影、管理员查询 API 和前端类型；输出包含读取时间、可空计数／近期分桶／配额观测，验证客户端、归一化、权限、缺失与错误路径测试通过，完成质量检查后提交。

## 2. 管理工作台

- [x] 2.1 以现有实例与账号操作为输入，重组实例选择、账号主视图、关联上游／日志次级视图；接入用量刷新和详情，补齐中英文与加载／错误／空状态，验证组件测试及现有操作回归通过，完成质量检查后提交。

## 3. 页面验收与交付

- [x] 3.1 以本地模拟管理接口和隔离数据库为输入，验证桌面、320px 与 390px 窄屏的布局、视图切换、用量语义及独立失败状态，保存实际截图；运行相关 E2E、类型、lint、格式和 OpenSpec 严格校验，修复发现的问题并提交最终改动。
