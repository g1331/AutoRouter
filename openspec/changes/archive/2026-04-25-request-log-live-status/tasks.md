## 1. 请求日志终态收口正确性

- [x] 1.1 梳理并修复代理中下游取消、客户端断开、流式中断等路径，确保对应 `request_logs` 行都能在同一次请求生命周期内收口为终态。
- [x] 1.2 为长时间未收口的进行中日志增加 stale reconciliation 兜底逻辑，确保超过阈值后能自动改写为可解释终态。
- [x] 1.3 扩展日志类型转换与衍生状态映射，确保已取消、已中断请求不会继续被派生为进行中生命周期状态。

## 2. 日志实时刷新链路

- [x] 2.1 在保持现有日志列表接口为主数据源的前提下，新增轻量实时刷新链路或等价失效通知机制，用于驱动进行中请求的短延迟状态更新。
- [x] 2.2 为日志页 Hook 与页面状态接入实时刷新能力，并在实时链路不可用时自动回退到定时刷新。

## 3. 日志页面用户体验收口

- [x] 3.1 更新日志页主状态展示，仅保留状态码；进行中请求只显示加载指示且不显示占位符 `-`，并在终态时停止加载指示。
- [x] 3.2 为日志页补充实时更新提示与必要的轻量变化反馈，确保用户能感知“状态刚刚发生变化”。
- [x] 3.3 调整相关中英文文案与错误呈现，确保不再叠加状态标签，并让上游错误信息以明确错误色突出。

## 4. 统计口径与验证

- [x] 4.1 调整依赖 `request_logs` 的相关聚合逻辑，确保已取消、已中断和 stale 收口请求不再长期计入进行中统计，且不污染成功类性能均值。
- [x] 4.2 补充测试覆盖取消前断开、已发往上游后的中断、流式半途中断、stale 自动收口、实时刷新降级与统计排除逻辑。
- [x] 4.3 运行质量门禁并形成可提交状态：`pnpm lint`、`pnpm exec tsc --noEmit`、`pnpm test:run`。
