## 1. 数据契约与兼容迁移

- [x] 1.1 设计并落地上游模型发现配置、目录缓存与统一模型规则结构，更新数据库 schema、API 类型与 transformer，同时定义旧 `allowed_models` / `model_redirects` 到新结构的兼容读取策略。
- [x] 1.2 扩展 upstream CRUD 与管理端请求校验，支持 discovery config、catalog cache 元数据与 exact / regex / alias 规则的读写，并补充对应的服务层与接口测试。

## 2. 模型发现与目录缓存链路

- [x] 2.1 实现按上游发现方式执行模型抓取的服务层，覆盖 OpenAI 兼容、Anthropic、Gemini 原生、Gemini OpenAI 兼容与自定义发现端点。
- [x] 2.2 为模型抓取结果建立统一规范化与持久化流程，记录来源、抓取时间、最近成功状态与失败原因，并补充抓取成功/失败测试。
- [x] 2.3 实现原生发现失败后的 LiteLLM fallback 候选装配逻辑，确保返回结果带有推断来源标记且不会伪装成上游真实目录。
- [x] 2.4 修正 discovery 对带路径 API 根地址与相对自定义端点的 URL 拼接，确保模型抓取继续命中已保存的上游前缀路径。

## 3. 管理端工作台与导入流程

- [x] 3.1 重构上游表单与工作台的模型相关区域，拆分出“模型发现”与“模型规则”两个区块，并展示最近抓取状态、来源标签与规则摘要。
- [x] 3.2 实现目录浏览与批量导入交互，让管理员能够从原生发现或 LiteLLM 候选目录中选择模型并导入为显式规则。
- [x] 3.3 补充 hooks、表单组件与工作台组件测试，覆盖状态展示、来源区分、导入回显与规则编辑场景。

## 4. 运行时候选过滤与验证

- [x] 4.1 在 proxy 主链路中接入显式模型规则过滤，将其放在路径能力匹配之后、负载均衡选择之前，并保证 exact / regex / alias 未命中时不会继续保留该上游候选。
- [x] 4.2 补充模型规则过滤相关的日志与观测字段，确保能够区分“能力不匹配”“模型规则未命中”“健康/配额/并发排除”等候选淘汰原因。
- [x] 4.3 完成模型发现、目录导入、严格候选过滤与 fallback 来源标记的单元/接口/关键 UI 验证，并跑通本次变更涉及的质量门禁。
