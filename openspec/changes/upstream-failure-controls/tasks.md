## 1. OpenSpec 与数据契约

- [x] 1.1 补齐 OpenSpec proposal、design、specs 和 tasks，并通过 `openspec validate` 验证变更文档
- [x] 1.2 扩展 PostgreSQL 与 SQLite schema，新增失败规则表和上游全局规则开关配置，并生成对应迁移
- [x] 1.3 扩展 API 类型、转换层和熔断默认配置，使响应超时与失败规则字段能创建、更新、读取和回显

## 2. 失败规则运行时

- [x] 2.1 新增失败规则服务，支持全局规则、本地规则、上游关闭全局规则，以及状态码、错误类型、响应头和响应体匹配
- [x] 2.2 在代理故障转移路径接入失败规则判定，命中规则时继续重试但不计入熔断，并把规则证据写入 failover history
- [x] 2.3 新增 Admin 失败规则 API，支持全局规则列表、创建、更新和删除，以及上游本地规则读写

## 3. 响应超时运行时

- [x] 3.1 在代理客户端中实现首字超时，复用 TTFT 有效输出事件判定，纯元数据事件不得解除首字等待
- [x] 3.2 在流式响应包装中实现空闲超时，超时后释放连接、标记失败、记录熔断并发送标准 SSE 错误事件
- [x] 3.3 扩展失败类型、统一错误证据和日志终态，区分 `first_byte_timeout`、`stream_idle_timeout`、普通超时和下游断开

## 4. 管理端与展示

- [x] 4.1 更新上游配置弹窗，增加首字超时、响应空闲超时、使用全局失败规则开关和本地失败规则编辑
- [x] 4.2 更新日志表格、路由决策时间线和中英文文案，展示规则命中、失败类型和是否计入熔断
- [x] 4.3 更新前端表单归一化和脏值比较，确保新增配置不会产生误报未保存状态
- [x] 4.4 在系统设置中新增全局失败规则管理入口和页面，支持列表、新增、启用、停用和删除全局规则
- [ ] 4.5 在失败规则编辑器中增加正则语法校验和示例文本匹配预览

## 5. 测试与校验

- [x] 5.1 增加失败规则服务、代理客户端和代理路由单元测试，覆盖规则命中、首字超时、空闲超时和熔断计数
- [x] 5.2 增加管理端组件测试，覆盖新增配置回显、编辑和提交 payload
- [x] 5.3 增加全局失败规则管理页面、编辑器全局模式和 hooks 测试
- [ ] 5.4 增加正则校验、预览匹配和禁用提交的组件测试
- [x] 5.5 运行 `pnpm format:check`、`git diff --check`、`pnpm lint`、`pnpm exec tsc --noEmit --pretty false`、`pnpm test:run --coverage` 和 `pnpm db:check:consistency`
