name: Code-Argus Review

on:
  pull_request:
    types: [opened, reopened]
  issue_comment:
    types: [created]

env:
  CODEX_API_KEY: ${{ secrets.CODEX_API_KEY }}
  CODEX_BASE_URL: ${{ secrets.CODEX_BASE_URL }}
  CODEX_MODEL: ${{ vars.CODEX_MODEL }}
  MAX_FIX_ROUNDS: ${{ vars.MAX_FIX_ROUNDS || '3' }}

concurrency:
  group: code-argus-${{ github.event.pull_request.number || github.event.issue.number || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: write  # 需要 write 权限来触发 repository_dispatch
  issues: write
  pull-requests: write

jobs:
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    # PR 自动触发 或 PR 评论中包含触发关键词（排除 bot 用户避免循环触发）
    if: |
      github.actor != 'github-actions[bot]' &&
      (github.event_name == 'pull_request' ||
       (github.event.issue.pull_request &&
        (contains(github.event.comment.body, 'code-argus review') ||
         contains(github.event.comment.body, 'code-argus fix-now') ||
         contains(github.event.comment.body, 'code-argus fix') ||
         contains(github.event.comment.body, 'argus review') ||
         contains(github.event.comment.body, 'argus fix-now') ||
         contains(github.event.comment.body, 'argus fix') ||
         contains(github.event.comment.body, 'code-argus 审查') ||
         contains(github.event.comment.body, 'code-argus 立即修复') ||
         contains(github.event.comment.body, 'code-argus 修复'))))

    outputs:
      allowed: ${{ steps.check_perm.outputs.allowed }}
      pr_number: ${{ steps.prmeta.outputs.number }}
      base_ref: ${{ steps.prmeta.outputs.base_ref }}
      auto_fix: ${{ steps.mode.outputs.auto_fix }}
      fix_now: ${{ steps.mode.outputs.fix_now }}
      current_round: ${{ steps.round.outputs.current }}

    steps:
      # ============================================================
      # Step 1: 权限检查 - 仅 collaborators 可触发
      # ============================================================
      - name: Check permissions
        id: check_perm
        uses: actions/github-script@v7
        with:
          script: |
            try {
              const { data } = await github.rest.repos.getCollaboratorPermissionLevel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                username: context.actor
              });
              const allowed = ['admin', 'write'].includes(data.permission);
              core.setOutput('allowed', allowed);
              if (!allowed) {
                console.log(`User ${context.actor} has ${data.permission} permission, skipping review`);
              }
              return allowed;
            } catch (error) {
              console.log(`Permission check failed: ${error.message}`);
              core.setOutput('allowed', false);
              return false;
            }

      - name: Add reaction to trigger comment
        if: steps.check_perm.outputs.allowed == 'true' && github.event_name == 'issue_comment'
        uses: actions/github-script@v7
        with:
          script: |
            try {
              await github.rest.reactions.createForIssueComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: context.payload.comment.id,
                content: 'eyes'
              });
            } catch (error) {
              console.log(`Failed to add reaction: ${error.message}`);
            }

      - name: Get PR metadata
        if: steps.check_perm.outputs.allowed == 'true'
        id: prmeta
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.pull_request?.number ?? context.payload.issue?.number;

            if (!prNumber) {
              core.setOutput('number', '');
              core.setOutput('base_ref', '');
              return;
            }

            // pull_request 事件可直接拿到 base.ref；issue_comment 事件需要额外查询
            let baseRef = context.payload.pull_request?.base?.ref;
            if (!baseRef) {
              const { data: pr } = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber
              });
              baseRef = pr.base.ref;
            }

            core.setOutput('number', prNumber.toString());
            core.setOutput('base_ref', baseRef || '');

      - name: Determine auto-fix mode
        id: mode
        if: steps.check_perm.outputs.allowed == 'true'
        run: |
          if [ "${{ github.event_name }}" = "issue_comment" ]; then
            COMMENT_BODY="${{ github.event.comment.body }}"
            # 检测 fix-now 模式（跳过审查直接修复）
            if echo "$COMMENT_BODY" | grep -qiE "(code-argus fix-now|argus fix-now|code-argus 立即修复)"; then
              echo "fix_now=true" >> $GITHUB_OUTPUT
              echo "auto_fix=true" >> $GITHUB_OUTPUT
            elif echo "$COMMENT_BODY" | grep -qiE "(code-argus fix|argus fix|code-argus 修复)"; then
              echo "fix_now=false" >> $GITHUB_OUTPUT
              echo "auto_fix=true" >> $GITHUB_OUTPUT
            else
              echo "fix_now=false" >> $GITHUB_OUTPUT
              echo "auto_fix=false" >> $GITHUB_OUTPUT
            fi
          else
            # PR opened/reopened - 默认只 review 不修复
            echo "fix_now=false" >> $GITHUB_OUTPUT
            echo "auto_fix=false" >> $GITHUB_OUTPUT
          fi

      - name: Determine current round
        id: round
        if: steps.check_perm.outputs.allowed == 'true' && steps.mode.outputs.auto_fix == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.issue?.number;
            if (!prNumber) {
              core.setOutput('current', '1');
              return;
            }

            // 统计 PR 评论中已有的 Auto-Fix Round 标记数量
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              per_page: 100
            });

            let maxRound = 0;
            const roundPattern = /\[Code-Argus Auto-Fix Round (\d+)/;
            for (const comment of comments) {
              const match = comment.body?.match(roundPattern);
              if (match) {
                maxRound = Math.max(maxRound, parseInt(match[1], 10));
              }
            }

            core.setOutput('current', (maxRound + 1).toString());

  review:
    name: Review
    runs-on: ubuntu-latest
    needs: prepare
    # 跳过 fix-now 模式（直接使用上次审查结果）
    if: needs.prepare.outputs.allowed == 'true' && needs.prepare.outputs.fix_now != 'true'
    timeout-minutes: 45

    outputs:
      review_output: ${{ steps.review.outputs.final-message }}

    steps:
      # ============================================================
      # Step 2: Checkout PR 代码
      # ============================================================
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch PR refs
        run: |
          git fetch --no-tags origin ${{ needs.prepare.outputs.base_ref }}
          git fetch --no-tags origin +refs/pull/${{ needs.prepare.outputs.pr_number }}/head:refs/remotes/origin/pr-head

      - name: Checkout PR head
        run: git checkout origin/pr-head

      # ============================================================
      # Step 3: 准备 Review 输入
      # ============================================================
      - name: Load config
        id: config
        run: |
          MAX_COMMENTS=10
          MIN_SEVERITY=low
          LANGUAGE=auto
          MODEL="${CODEX_MODEL:-gpt-5.2}"

          if [ -f ".github/reviewbot.yaml" ]; then
            if command -v yq &> /dev/null; then
              MAX_COMMENTS=$(yq '.max_comments // 10' .github/reviewbot.yaml)
              MIN_SEVERITY=$(yq '.min_severity // "low"' .github/reviewbot.yaml)
              LANGUAGE=$(yq '.language // "auto"' .github/reviewbot.yaml)
            fi
          fi

          echo "max_comments=$MAX_COMMENTS" >> $GITHUB_OUTPUT
          echo "min_severity=$MIN_SEVERITY" >> $GITHUB_OUTPUT
          echo "language=$LANGUAGE" >> $GITHUB_OUTPUT
          echo "model=$MODEL" >> $GITHUB_OUTPUT

      - name: Fetch PR title/body
        uses: actions/github-script@v7
        env:
          PR_NUMBER: ${{ needs.prepare.outputs.pr_number }}
        with:
          script: |
            const fs = require('fs');
            const prNumber = parseInt(process.env.PR_NUMBER, 10);
            if (!prNumber || Number.isNaN(prNumber)) {
              fs.writeFileSync('/tmp/pr_title.txt', '', 'utf8');
              fs.writeFileSync('/tmp/pr_body.txt', '', 'utf8');
              return;
            }

            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });

            fs.writeFileSync('/tmp/pr_title.txt', pr.title || '', 'utf8');
            fs.writeFileSync('/tmp/pr_body.txt', pr.body || '', 'utf8');

      - name: Prepare prompt file
        run: |
          mkdir -p .github/codex/prompts

          # 获取 diff 并保存到临时文件（避免 shell 变量长度限制）
          git diff origin/${{ needs.prepare.outputs.base_ref }}...origin/pr-head > /tmp/pr_diff.txt

          # 生成 prompt 文件
          cat > .github/codex/prompts/review.md << 'PROMPT_EOF'
          # Code-Argus Review Instructions

          You are Code-Argus, an expert code reviewer focused on high-impact issues.

          ## Core Principles

          1. **High signal-to-noise**: Only comment if it would likely change a merge decision
          2. **No style nits**: Never comment on formatting, naming conventions, or subjective preferences
          3. **Actionable feedback**: Every comment must include a concrete fix

          ## Review Focus Areas

          - **Correctness**: Logic errors, edge cases, null handling, race conditions
          - **Security**: XSS, injection, auth bypass, sensitive data exposure
          - **Architecture**: Breaking changes, API compatibility, cross-system impact
          - **Testing**: Missing tests for critical paths, inadequate coverage

          Do NOT comment on:
          - Code style, formatting, or naming conventions
          - Minor optimizations that don't affect functionality
          - Personal preferences or "nice to have" suggestions

          ## Configuration

          PROMPT_EOF

          echo "- Maximum comments: ${{ steps.config.outputs.max_comments }}" >> .github/codex/prompts/review.md
          echo "- Minimum severity: ${{ steps.config.outputs.min_severity }}" >> .github/codex/prompts/review.md
          echo "- Language: ${{ steps.config.outputs.language }} (if 'auto', use the same language as the PR)" >> .github/codex/prompts/review.md
          echo "" >> .github/codex/prompts/review.md

          cat >> .github/codex/prompts/review.md << 'PROMPT_EOF'
          ## Output Format

          Return a JSON object with this exact structure:

          ```json
          {
            "summary": {
              "total_issues": 0,
              "high": 0,
              "medium": 0,
              "low": 0,
              "focus_areas": []
            },
            "comments": []
          }
          ```

          Each comment in the array should have:
          - `file`: path to the file (e.g., "src/api/routes.ts")
          - `line_start`: starting line number
          - `line_end`: ending line number (same as line_start if single line)
          - `severity`: "high", "medium", or "low"
          - `title`: brief issue title
          - `description`: why this is a problem
          - `suggestion`: the fixed code (will be shown as GitHub suggested change)

          ## Instructions

          1. You MAY read any file in the repository and run searches to understand context
          2. Review ONLY the changes in this PR (diff provided below); use other files only for context and impact verification
          3. Sort issues by severity (high first), then limit to max_comments
          4. If language is 'auto', respond in the same language as the PR title/description
          5. If no significant issues found, return empty comments array
          6. Output ONLY the JSON object, no other text

          ## PR Information

          PROMPT_EOF

          echo -n "**Title**: " >> .github/codex/prompts/review.md
          cat /tmp/pr_title.txt >> .github/codex/prompts/review.md
          echo "" >> .github/codex/prompts/review.md
          echo "**Description**:" >> .github/codex/prompts/review.md
          cat /tmp/pr_body.txt >> .github/codex/prompts/review.md
          echo "" >> .github/codex/prompts/review.md
          echo "## PR Diff" >> .github/codex/prompts/review.md
          echo "" >> .github/codex/prompts/review.md
          echo '```diff' >> .github/codex/prompts/review.md
          cat /tmp/pr_diff.txt >> .github/codex/prompts/review.md
          echo '```' >> .github/codex/prompts/review.md

      # ============================================================
      # Step 4: 运行 Code-Argus Review (使用官方 Action)
      # ============================================================
      - name: Run Code-Argus
        id: review
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.CODEX_API_KEY }}
          # CODEX_BASE_URL 格式: https://xxx.com/v1，需要加 /responses
          responses-api-endpoint: ${{ secrets.CODEX_BASE_URL }}/responses
          model: ${{ steps.config.outputs.model }}
          effort: xhigh
          sandbox: read-only
          prompt-file: .github/codex/prompts/review.md
          output-schema: |
            {
              "type": "object",
              "properties": {
                "summary": {
                  "type": "object",
                  "properties": {
                    "total_issues": { "type": "integer" },
                    "high": { "type": "integer" },
                    "medium": { "type": "integer" },
                    "low": { "type": "integer" },
                    "focus_areas": { "type": "array", "items": { "type": "string" } }
                  },
                  "required": ["total_issues", "high", "medium", "low", "focus_areas"],
                  "additionalProperties": false
                },
                "comments": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "file": { "type": "string" },
                      "line_start": { "type": "integer" },
                      "line_end": { "type": "integer" },
                      "severity": { "type": "string", "enum": ["high", "medium", "low"] },
                      "title": { "type": "string" },
                      "description": { "type": "string" },
                      "suggestion": { "type": "string" }
                    },
                    "required": ["file", "line_start", "line_end", "severity", "title", "description", "suggestion"],
                    "additionalProperties": false
                  }
                }
              },
              "required": ["summary", "comments"],
              "additionalProperties": false
            }

  publish:
    name: Publish
    runs-on: ubuntu-latest
    needs: [prepare, review]
    if: needs.prepare.outputs.allowed == 'true' && always()

    steps:
      - name: Post review comments
        id: post
        uses: actions/github-script@v7
        env:
          REVIEW_OUTPUT: ${{ needs.review.outputs.review_output }}
          PR_NUMBER: ${{ needs.prepare.outputs.pr_number }}
          REVIEW_RESULT: ${{ needs.review.result }}
          AUTO_FIX: ${{ needs.prepare.outputs.auto_fix }}
          CURRENT_ROUND: ${{ needs.prepare.outputs.current_round || '1' }}
          MAX_FIX_ROUNDS: ${{ env.MAX_FIX_ROUNDS }}
        with:
          script: |
            const prNumber = parseInt(process.env.PR_NUMBER, 10);
            if (!prNumber || Number.isNaN(prNumber)) return;

            const reviewResult = process.env.REVIEW_RESULT || '';
            const output = process.env.REVIEW_OUTPUT || '';
            const autoFix = process.env.AUTO_FIX === 'true';
            const currentRound = parseInt(process.env.CURRENT_ROUND, 10) || 1;
            const maxRounds = parseInt(process.env.MAX_FIX_ROUNDS, 10) || 3;

            if (reviewResult !== 'success') {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: `## Code-Argus Review\n\n❌ Review failed. Please check the [workflow logs](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) for details.\n\nComment \`code-argus review\` to retry.`
              });
              return;
            }

            // 检查是否为空输出
            if (!output || output.trim() === '') {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: `## Code-Argus Review\n\n❌ No output received from review service.\n\nComment \`code-argus review\` to retry.`
              });
              return;
            }

            let review;

            try {
              review = JSON.parse(output.trim());
            } catch (directParseError) {
              try {
                let jsonStr = null;
                const jsonMatch = output.match(/\{[\s\S]*"summary"[\s\S]*"comments"[\s\S]*\}(?=[^}]*$)/);
                if (jsonMatch) {
                  jsonStr = jsonMatch[0];
                }
                if (!jsonStr) {
                  throw new Error('No JSON object found in output');
                }
                review = JSON.parse(jsonStr);
              } catch (extractError) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: prNumber,
                  body: `## Code-Argus Review\n\n❌ Failed to parse review output.\n\nComment \`code-argus review\` to retry.`
                });
                return;
              }
            }

            const severityEmoji = { high: '🔴', medium: '🟡', low: '🟢' };

            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });
            const commitSha = pr.head.sha;

            const reviewComments = [];
            if (review.comments && review.comments.length > 0) {
              for (const comment of review.comments) {
                const emoji = severityEmoji[comment.severity] || '🟡';
                let body = `## ${emoji} [${comment.severity.toUpperCase()}] ${comment.title}\n\n`;
                body += `${comment.description}\n\n`;

                if (comment.suggestion) {
                  body += '```suggestion\n';
                  body += comment.suggestion;
                  if (!comment.suggestion.endsWith('\n')) body += '\n';
                  body += '```\n';
                }

                const lineEnd = comment.line_end || comment.line_start;
                const reviewComment = {
                  path: comment.file,
                  line: lineEnd,
                  side: 'RIGHT',
                  body
                };

                if (comment.line_start && comment.line_end && comment.line_start !== comment.line_end) {
                  reviewComment.start_line = comment.line_start;
                  reviewComment.start_side = 'RIGHT';
                }

                reviewComments.push(reviewComment);
              }
            }

            // 生成总结
            const summary = review.summary || { total_issues: 0, high: 0, medium: 0, low: 0, focus_areas: [] };
            const roundInfo = autoFix ? ` (Round ${currentRound}/${maxRounds})` : '';
            let summaryBody = `## Code-Argus Review${roundInfo}\n\n`;
            let shouldTriggerFix = false;

            if (summary.total_issues === 0) {
              summaryBody += `✅ Review completed. **No significant issues found.**\n\n`;
              summaryBody += `The changes look good from a correctness, security, and architecture perspective.\n`;
            } else {
              summaryBody += `Review completed. **${summary.total_issues}** issue(s) found.\n\n`;
              summaryBody += `| Severity | Count |\n`;
              summaryBody += `|----------|-------|\n`;
              if (summary.high > 0) summaryBody += `| 🔴 High | ${summary.high} |\n`;
              if (summary.medium > 0) summaryBody += `| 🟡 Medium | ${summary.medium} |\n`;
              if (summary.low > 0) summaryBody += `| 🟢 Low | ${summary.low} |\n`;

              if (summary.focus_areas && summary.focus_areas.length > 0) {
                summaryBody += `\n**Focus areas**: ${summary.focus_areas.join(', ')}\n`;
              }

              // 根据模式决定行为
              if (autoFix && currentRound <= maxRounds) {
                summaryBody += `\n🔧 **Auto-fix mode**: Triggering @autorouter-bot to fix issues...\n`;
                shouldTriggerFix = true;
              } else if (autoFix && currentRound > maxRounds) {
                summaryBody += `\n⚠️ **Max rounds reached** (${maxRounds}). Please fix remaining issues manually.\n`;
              } else {
                // 非 auto-fix 模式，显示手动触发提示
                summaryBody += `\n---\n`;
                summaryBody += `💡 **Tip**: Comment the following to trigger auto-fix:\n`;
                summaryBody += `\`\`\`\n@autorouter-bot 请根据上方 Code-Argus review 反馈修复代码问题\n\`\`\`\n`;
              }
            }

            summaryBody += `\n---\n`;
            summaryBody += `Comment \`code-argus review\` to re-trigger review | \`code-argus fix\` for auto-fix loop`;

            // 输出是否需要触发修复
            core.setOutput('should_trigger_fix', shouldTriggerFix.toString());

            // 提交 Review
            let reviewCreated = false;

            try {
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
                commit_id: commitSha,
                event: 'COMMENT',
                body: summaryBody,
                comments: reviewComments.length > 0 ? reviewComments : undefined
              });
              reviewCreated = true;
            } catch (e) {
              console.log(`Failed to create review with inline comments: ${e.message}`);
            }

            if (!reviewCreated) {
              try {
                await github.rest.pulls.createReview({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: prNumber,
                  commit_id: commitSha,
                  event: 'COMMENT',
                  body: summaryBody
                });
                reviewCreated = true;
              } catch (e) {
                console.log(`Failed to create summary review: ${e.message}`);
              }

              if (reviewComments.length > 0) {
                for (const comment of reviewComments) {
                  try {
                    await github.rest.pulls.createReviewComment({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      pull_number: prNumber,
                      commit_id: commitSha,
                      path: comment.path,
                      line: comment.line,
                      side: comment.side,
                      start_line: comment.start_line,
                      start_side: comment.start_side,
                      body: comment.body
                    });
                  } catch (e) {
                    console.log(`Failed to post comment on ${comment.path}:${comment.line}: ${e.message}`);
                  }
                }
              }

              if (!reviewCreated) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: prNumber,
                  body: summaryBody
                });
              }
            }

      - name: Trigger auto-fix via repository_dispatch
        if: steps.post.outputs.should_trigger_fix == 'true'
        uses: actions/github-script@v7
        env:
          PR_NUMBER: ${{ needs.prepare.outputs.pr_number }}
          CURRENT_ROUND: ${{ needs.prepare.outputs.current_round || '1' }}
          MAX_FIX_ROUNDS: ${{ env.MAX_FIX_ROUNDS }}
          HEAD_REF: ${{ needs.prepare.outputs.head_ref }}
        with:
          script: |
            const prNumber = parseInt(process.env.PR_NUMBER, 10);
            const currentRound = parseInt(process.env.CURRENT_ROUND, 10) || 1;
            const maxRounds = parseInt(process.env.MAX_FIX_ROUNDS, 10) || 3;

            // 获取 PR head ref
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });

            // 发送 repository_dispatch 触发 claude-codex-agent
            await github.rest.repos.createDispatchEvent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              event_type: 'code-argus-fix',
              client_payload: {
                pr_number: prNumber,
                current_round: currentRound,
                max_rounds: maxRounds,
                head_ref: pr.head.ref
              }
            });

            // 添加通知评论
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: [
                `🔧 **Code-Argus Auto-Fix** (Round ${currentRound}/${maxRounds})`,
                '',
                '正在触发自动修复...',
                '',
                `查看 [Actions](https://github.com/${context.repo.owner}/${context.repo.repo}/actions) 跟踪进度。`
              ].join('\n')
            });

  # ============================================================
  # fix-now: 跳过审查，直接触发修复
  # ============================================================
  fix-now:
    name: Fix Now
    runs-on: ubuntu-latest
    needs: prepare
    if: needs.prepare.outputs.allowed == 'true' && needs.prepare.outputs.fix_now == 'true'

    steps:
      - name: Trigger fix directly
        uses: actions/github-script@v7
        env:
          PR_NUMBER: ${{ needs.prepare.outputs.pr_number }}
          CURRENT_ROUND: ${{ needs.prepare.outputs.current_round || '1' }}
          MAX_FIX_ROUNDS: ${{ env.MAX_FIX_ROUNDS }}
        with:
          script: |
            const prNumber = parseInt(process.env.PR_NUMBER, 10);
            const currentRound = parseInt(process.env.CURRENT_ROUND, 10) || 1;
            const maxRounds = parseInt(process.env.MAX_FIX_ROUNDS, 10) || 3;

            // 获取 PR head ref（agent 会自己读取 PR reviews 来了解需要修复什么）
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });

            // 发送 repository_dispatch 触发修复
            await github.rest.repos.createDispatchEvent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              event_type: 'code-argus-fix',
              client_payload: {
                pr_number: prNumber,
                current_round: currentRound,
                max_rounds: maxRounds,
                head_ref: pr.head.ref
              }
            });

            // 添加通知评论
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: [
                `🔧 **Code-Argus Fix-Now** (Round ${currentRound}/${maxRounds})`,
                '',
                '正在触发自动修复（跳过审查）...',
                '',
                `查看 [Actions](https://github.com/${context.repo.owner}/${context.repo.repo}/actions) 跟踪进度。`
              ].join('\n')
            });
