# AutoRouter 代理上下文

本上下文统一描述 AutoRouter 代理请求的路由与执行概念，避免把网关决策、上游响应和客户端体验混为一谈。

## 代理路由

**路由决策延迟**：代理请求进入网关后，到开始向选定上游发起请求之前，认证、准入、候选构建、能力匹配、熔断检查、排队与选路所消耗的时间。
_避免_：总响应延迟、上游响应延迟、TTFT

## 请求日志筛选

**请求日志筛选**：对请求日志集合施加的可组合条件，包括主体范围、状态、模型、时间窗口和性能信号；同一组筛选语义可用于日志列表与窗口统计。
_避免_：把请求日志筛选等同于日志展示；排序和分页属于读取方式，不改变筛选集合。

**时间窗口优先级**：显式起止时间优先于 preset；preset 仅在显式起始边界缺失时提供下界，`all` 不设下界。

**状态条件优先级**：精确 `status_code` 优先于 `status_class`；只有没有精确状态码时，状态类别才参与筛选。

**窗口统计投影**：窗口统计保留请求日志筛选条件，不包含单条日志 `id`、分页或排序；它描述筛选窗口而不是当前页。

**自定义时间窗口**：只有完整的起止边界才构成有效筛选；不完整输入不得静默退化为 `all`，也不得作为有效窗口提交。

**性能信号**：请求日志筛选可按延迟与吞吐条件缩小集合；用户界面的 preset 只是选择既定性能信号的呈现方式，不构成独立的查询语义。

**筛选链接恢复**：外部筛选链接中的无效条件被丢弃；无效或不完整时间窗口回退到 `30d`，不得静默扩大为 `all`。
